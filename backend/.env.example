# Database Configuration
# Replace 'username' and 'password' with your PostgreSQL credentials
DATABASE_URL="postgresql://username:password@localhost:5432/personal_context_hub?schema=public"

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
# JWT_SECRET: A secret key for signing authentication tokens (NOT an API key!)
# Generate a secure random key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or just use any long random string (at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-a-long-random-string
JWT_EXPIRES_IN=7d

# AI Provider Configuration (Choose ONE)
# The system will automatically use the first configured provider in priority order:
# 1. Groq (FREE, fastest) - RECOMMENDED
# 2. OpenRouter (cheapest paid)
# 3. Grok (X.AI)
# 4. OpenAI (fallback)

# Option 1: Groq API (RECOMMENDED - FREE)
# Get your FREE API key from: https://console.groq.com
# No credit card required, 14,400 requests/day free tier
GROQ_API_KEY=

# Option 2: OpenRouter (Cheapest paid option - ~$0.50/month)
# Get your API key from: https://openrouter.ai/keys
# Pay only for what you use, many models available
OPENROUTER_API_KEY=

# Option 3: Grok API (X.AI - $8/month + $25 credits)
# Get your API key from: https://console.x.ai/
# Requires X Premium subscription
GROK_API_KEY=

# Option 4: OpenAI (Most expensive - ~$10-20/month)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: Override default model for chosen provider
# Examples:
#   Groq: llama-3.1-70b-versatile, mixtral-8x7b-32768
#   OpenRouter: meta-llama/llama-3.1-8b-instruct, anthropic/claude-3.5-haiku
#   OpenAI: gpt-4o-mini, gpt-4o
AI_MODEL=

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,chrome-extension://*

# Frontend URL (for OAuth callbacks and emails)
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001

# Google OAuth (Optional - see GOOGLE_OAUTH_SETUP.md)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
SESSION_SECRET=

# Email Service (Optional - see DEPLOYMENT.md)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@yourapp.com
